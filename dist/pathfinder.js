export function searchPath(seeker, waypoint, tileset, settings) {
    let round = 0;
    const seekerColor = document.getElementById(`${seeker[0]}-${seeker[1]}`).childNodes[0].style.background;
    console.log(seekerColor);
}
